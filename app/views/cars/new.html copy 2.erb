<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Car - <%= @car.brand.name %> <%= @car.model.name %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex flex-col min-h-screen">

<nav class="bg-blue-900 text-white p-4 flex justify-between items-center w-full fixed top-0 left-0 z-50">
    <div class="text-2xl font-bold">Elite Motors</div>
    <ul class="flex space-x-6">
        <li><%= link_to 'Home', root_path, class: 'hover:underline' %></li>
        <li><%= link_to 'Add Car', new_car_path, class: 'hover:underline' %></li>
        <li><%= link_to 'Back to Car', car_path(@car), class: 'hover:underline' %></li>
    </ul>
</nav>

<main class="flex-grow flex flex-col items-center mt-20 w-full">
    <div class="bg-white shadow-lg rounded-lg w-full max-w-4xl p-6">
        <h2 class="text-3xl font-bold text-center mb-6">Edit Car</h2>

        <%= form_with model: @car, local: true do |f| %>
            <div class="mb-4">
                <%= f.label :price, "Price", class: "block font-bold" %>
                <%= f.text_field :price, class: "w-full p-2 border rounded" %>
            </div>

            <div class="mb-4">
                <%= f.label :km, "Kilometers", class: "block font-bold" %>
                <%= f.text_field :km, class: "w-full p-2 border rounded" %>
            </div>

            <div class="mb-4">
                <%= f.label :fuel_type, "Fuel Type", class: "block font-bold" %>
                <%= f.text_field :fuel_type, class: "w-full p-2 border rounded" %>
            </div>

            <div class="mb-4">
                <%= f.label :transmission, "Transmission", class: "block font-bold" %>
                <%= f.text_field :transmission, class: "w-full p-2 border rounded" %>
            </div>

            <div class="mb-4">
                <%= f.label :power, "Power (hp)", class: "block font-bold" %>
                <%= f.text_field :power, class: "w-full p-2 border rounded" %>
            </div>

            <div class="mb-4">
                <%= f.label :color, "Color", class: "block font-bold" %>
                <%= f.text_field :color, class: "w-full p-2 border rounded" %>
            </div>

            <div class="mb-4">
                <%= f.label :description, "Description", class: "block font-bold" %>
                <%= f.text_area :description, class: "w-full p-2 border rounded" %>
            </div>

            <div class="flex justify-between">
                <%= link_to 'Cancel', car_path(@car), class: "bg-gray-500 text-white p-3 rounded hover:bg-gray-600" %>
                <%= f.submit "Update Car", class: "bg-blue-500 text-white p-3 rounded hover:bg-blue-600" %>
            </div>
        <% end %>
    </div>
</main>

<footer class="bg-gray-800 text-white text-center p-4 w-full fixed bottom-0 left-0">
    &copy; 2025 Elite Motors. All rights reserved.
</footer>

</body>
</html>
