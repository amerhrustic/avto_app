<!DOCTYPE html>
<html lang="sl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Registracija</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbolinks-track": "reload" %>
</head>
<body>
  <!-- Navbar -->
  <nav class="bg-blue-900 text-white p-4 flex justify-between items-center w-full fixed top-0 left-0 z-50">
    <div class="text-2xl font-bold">Elite Motors</div>
    <ul class="flex space-x-6">
      <li><%= link_to 'Home', root_path, class: 'hover:underline' %></li>
      <% if logged_in? %>
        <li><%= link_to 'Add Car', new_car_path, class: 'hover:underline' %></li>
        <li class="font-semibold">Welcome, <%= current_user.name %></li>
        <li><%= link_to 'Logout', logout_path, method: :delete, class: 'hover:underline' %></li>
      <% else %>
        <li><%= link_to 'Login', login_path, class: 'hover:underline' %></li>
        <li><%= link_to 'Register', register_path, class: 'hover:underline' %></li>
      <% end %>
    </ul>
  </nav>

  <div class="min-h-screen flex items-center justify-center bg-gray-100 mt-20">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
      <h2 class="text-2xl font-bold text-center mb-6">Registracija</h2>

      <% if flash[:alert] %>
        <div class="bg-red-500 text-white p-3 rounded mb-4">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= form_with(model: @user, local: true) do |f| %>
        <div class="mb-4">
          <%= f.label :name, "Ime", class: "block text-sm font-medium text-gray-700" %>
          <%= f.text_field :name, class: "mt-1 block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <div class="mb-4">
          <%= f.label :email, "Email", class: "block text-sm font-medium text-gray-700" %>
          <%= f.email_field :email, class: "mt-1 block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <div class="mb-4">
          <%= f.label :password, "Geslo", class: "block text-sm font-medium text-gray-700" %>
          <%= f.password_field :password, class: "mt-1 block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <div class="mb-4">
          <%= f.label :password_confirmation, "Potrditev gesla", class: "block text-sm font-medium text-gray-700" %>
          <%= f.password_field :password_confirmation, class: "mt-1 block w-full p-3 border border-gray-300 rounded-lg shadow-sm focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <%= f.submit "Registriraj se", class: "w-full bg-blue-500 text-white p-3 rounded-lg hover:bg-blue-600 transition duration-300" %>
      <% end %>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white p-4 text-center mt-12">
    <p>&copy; 2025 Elite Motors. All rights reserved.</p>
  </footer>
</body>
</html>
